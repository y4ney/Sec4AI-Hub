---
层级: "第3层 — 智能体框架"
威胁等级: "高危"
序号: "AF-003"
OpenClaw 源码路径: "src/config/config.ts"
层级说明: "框架层面的威胁：工具滥用、会话派生、跨会话信息泄漏、沙箱逃逸"
---

# 跨会话数据泄漏

#### &#x20;威胁描述

如果 `dmScope` 配置不当，多用户私信会共享会话上下文。群组私信中的用户可能看到其他用户的消息，违反机密性原则。

#### 缓解措施

配置验证机制会检查 `dmScope` 设置并对潜在的数据泄漏发出警告。会话隔离是默认行为。访问组强制执行显式授权。
